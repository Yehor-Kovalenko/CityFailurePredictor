apiVersion: 1

providers:
  - name: CityFailurePredictor
    orgId: 1
    folder: CityFailurePredictor
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards